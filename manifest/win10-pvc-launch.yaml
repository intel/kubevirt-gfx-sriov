apiVersion: v1 
kind: PersistentVolume
metadata:
  name: pv-volume-win10
spec:
  storageClassName: manual
  capacity:
    storage: 30Gi  
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/vm/images/win10"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: winbootdisk
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi

---
apiVersion: v1
kind: Service
metadata:
  name: win10-pvc-ssh
  namespace: default
spec:
  ports:
    - name: win10-pvc-ssh
      protocol: TCP
      port: 22
      targetPort: 22
  selector:
    kubevirt.io/name: win10-pvc
  type: NodePort

---
apiVersion: v1
kind: Service
metadata:
  name: vm-rdp
  namespace: default
spec:
  ports:
    - port: 3389
      name: rdp
      protocol: TCP
      targetPort: 3389
  selector:
    kubevirt.io/name: win10-pvc
  externalTrafficPolicy: Cluster
  type: LoadBalancer

---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: win10-pvc
  namespace: default
spec:
  running: true
  template:
    metadata:
      labels:
        kubevirt.io/name: win10-pvc
    spec:
      domain:
        cpu:
          cores: 4
          model: host-passthrough
        memory:
          guest: "6G"
        devices:
          autoattachGraphicsDevice: true
          gpus:
          - name: igpu
            deviceName: intel.com/sriov-gpudevice
          disks:
          - name: bootdisk
            bootOrder: 1
            disk:
              bus: virtio
          autoattachPodInterface: true
          interfaces:
          - name: default
            masquerade: {}
        machine:
          type: q35
        firmware:
          bootloader:
            efi:
              secureBoot: false
      terminationGracePeriodSeconds: 0
      networks:
      - name: default
        pod: {}
      volumes:
      - name: bootdisk
        persistentVolumeClaim:
          claimName: winbootdisk
